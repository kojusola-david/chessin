services:
  chess-backend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/backend/node_modules
    working_dir: /app/backend
    environment:
      - NODE_ENV=development
    command: npm run dev